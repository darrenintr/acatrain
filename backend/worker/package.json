{
  "name": "acatrain-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test",
    "dev": "npx wrangler@4 dev",
    "deploy": "npx wrangler@4 deploy"
  }
}
